import React from 'react'
import styled from '@emotion/styled'

function ListHeader() {
  return <Header>제주도감</Header>
}

const Header = styled.header`
  position: fixed;
  top: 14px;
  font-weight: 800;
  font-size: 32px;
`

export default ListHeader
